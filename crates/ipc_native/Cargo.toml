[package]
name = "ipc_native"
version = "0.1.0"
edition = "2021"
description = "EmbeddedBrowser IPC native C ABI"
license = "MIT"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
ipc = { path = "../ipc" }

[dev-dependencies]
tempfile = "3.0"
