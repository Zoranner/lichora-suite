[package]
name = "ipc"
version = "0.1.0"
edition = "2021"
description = "EmbeddedBrowser IPC core"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
memmap2 = "0.9"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3.0"
