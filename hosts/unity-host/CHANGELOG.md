# Changelog

## 0.1.0

- Switch the Unity package runtime to the Lichora IPC v2 chain.
- Use typed IPC channels for control, status, input, frame and output data.
- Remove the old MemoryStacks dependency from the Unity package.
- Move the ECharts stress script into `Samples~` instead of the runtime script path.
- Align the package metadata and README with the current public API.

## 0.0.4

- Previous package version before the Rust IPC v2 release line.
