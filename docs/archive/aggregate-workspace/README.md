# Aggregate Workspace Archive

This directory preserves historical design and review material from the former `EmbeddedBrowser` aggregate workspace.

These documents are useful as background evidence for old performance issues, .NET-era implementation choices, and the transition into Rust IPC v2. They are not the current Lichora protocol source of truth.

Current Lichora entry points:

- `../../protocol.md`
- `../../development.md`
- `../../design/web-host-overlay.md`

