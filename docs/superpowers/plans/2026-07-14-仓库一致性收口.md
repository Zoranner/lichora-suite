# 仓库一致性收口实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**目标：** 让当前 `0.1.0` 的代码、Unity 资产、发布描述和质量门禁表达同一组已实现事实，消除冲突、遗漏与环境歧义。

**架构：** 不实现新的运行能力，以现有代码和发布 manifest 为事实源。分别收口 Unity 资产契约、Rust/overlay 可复现门禁和文档能力面，最后从仓库根执行统一验证。

**技术栈：** Rust/Cargo、Bun/TypeScript、Unity UPM/YAML、PowerShell、CSharpier。

**状态：** 已完成。仓库统一质量检查已通过；按仓库约束未运行 Unity Editor、Unity 内程序集编译或插件加载验证。

---

### Unity 资产元数据契约

**文件：**

- 修改：`scripts/check-plugin-contract.ps1`
- 纳入：`hosts/unity-host/Plugins/plugin-manifest.json.meta`
- 纳入：`hosts/unity-host/Scripts/Model.meta`
- 纳入：`hosts/unity-host/Scripts/Native.meta`
- 纳入：`hosts/unity-host/Scripts/Protocol.meta`
- 创建：`hosts/unity-host/Samples~/ECharts.meta`

- [x] 在插件契约脚本中递归检查 Unity Package 内文件和目录的 `.meta` 对应关系，排除名称以 `~` 结尾的 UPM 特殊目录本身。
- [x] 在补齐元数据前运行 `./scripts/check-plugin-contract.ps1`，确认检查能报告 4 个现有未跟踪元数据和 `Samples~/ECharts.meta`，共 5 个缺口。
- [x] 保留现有 4 个 `.meta` 的 GUID，清除尾随空白后纳入版本控制；为 `Samples~/ECharts.meta` 分配新的唯一 GUID。
- [x] 重新运行 `./scripts/check-plugin-contract.ps1`，预期输出 5 个插件通过且没有元数据缺口。

### Rust、overlay 与统一质量门禁

**文件：**

- 删除：`src/browser/handler.rs`
- 修改：`src/browser/mod.rs`
- 修改：`src/lib.rs`
- 修改：`packages/overlay/package.json`
- 创建：`packages/overlay/bun.lock`
- 修改：`.gitignore`
- 修改：`scripts/check-quality.ps1`

- [x] 用全仓引用扫描确认空 `PageHandler` 只有定义和 re-export，没有调用方。
- [x] 删除空实现和 re-export，并从 crate 能力说明中移除尚未落地的 DevTools 声明。
- [x] 将 overlay 包描述统一为 input ownership 语义，使用 Bun 添加本地 TypeScript 开发依赖并生成锁文件。
- [x] 在 overlay 检查前执行 `bun install --frozen-lockfile`，并忽略该包的 `node_modules`，确保类型检查使用锁定依赖版本。
- [x] 在 Rust `fmt`/`clippy` 后加入 `cargo test --no-default-features --all-targets`，让现有测试进入统一质量入口。
- [x] 运行 `cargo fmt --all`、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo test --no-default-features --all-targets` 和 `bun run check`。

### 当前能力与文档事实面

**文件：**

- 修改：`README.md`
- 修改：`docs/protocol.md`
- 修改：`docs/development.md`
- 修改：`hosts/unity-host/README.md`
- 修改：`docs/reviews/2026-07-10-本轮代码评审.md`
- 修改：`docs/superpowers/plans/2026-07-10-第一阶段工程门禁与发布契约.md`
- 修改：`docs/superpowers/plans/2026-07-10-Unity-native-facade-boundary.md`
- 修改：`docs/superpowers/plans/2026-07-10-Unity-Native程序集边界.md`

- [x] 以 control enum、实际 output 发布点、Unity 消费点和 plugin manifest 为证据，列出当前已实现、协议预留和未验证能力。
- [x] 修正根 README 的 Linux IPC、session/status、control、ScriptResult/PageEvent 和 DevTools 表述。
- [x] 在协议与开发文档中明确正式 `window.lichora` bridge 尚未注入，当前运行 transport 是 console fallback。
- [x] 保持 Unity host 的 Linux importer 描述与 manifest 的 `release: false` 一致。
- [x] 将代码评审报告改为“审查时发现、当前已整改、Unity 内仍未验证”的一致时间线。
- [x] 将第一阶段门禁计划和 facade 计划标记为已完成，将更早的 Native 程序集计划标记为已被 facade 计划取代。

### 集成验证与提交

**文件：**

- 修改：`docs/superpowers/plans/2026-07-14-仓库一致性收口.md`

- [x] 从仓库根运行 `./scripts/check-quality.ps1`。
- [x] 运行 `git diff --check`、Unity `.meta` 对应关系检查和能力关键词扫描。
- [x] 检查 `git status --short`，确认只包含计划范围内文件。
- [x] 将本计划标记为已完成并记录 Unity Editor、程序集编译、插件加载仍未验证。
- [x] 按仓库中文短标题风格拆分或合并为清晰逻辑提交。
